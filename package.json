{
  "name": "sholat-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create sholat_bot_db",
    "d1:apply:local": "wrangler d1 migrations apply sholat_bot_db --local",
    "d1:apply": "wrangler d1 migrations apply sholat_bot_db"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250129.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.57.0"
  }
}
